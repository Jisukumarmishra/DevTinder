express": "^5.2.1"
here ^ = is carate
1 = patch (small changes like small bug fixes)
2 = minor chages
5 = major chages (happens when breaking chages are there )

^5.2.1 = means our express is automatically be updated when chaes in the 5.x.x series
if we remove ~(we replace with tilda also in the palce of carate but that have diffrent meaning(only patch(minor) changes) ) or carate then its never auto update


package.lock.json = tell the exact version of the project locking on or uses 

## Monggodb Database ##

To Connect The Nodejs application to the the mongodb database we use thw npm library called mongoose
npm i mongoose

Route Handler = decides which function runs
app.get('/user', ...)

Request Handler = function that handles the request
(req, res) => {
    res.send("User data");
}

Does actual work:

DB call
Logic
Send response

middleware is needed one of the reason is to autenticate the user cofidentatial data, code readiblity


“Route handler defines the endpoint and HTTP method, while request handler is the function executed when that route is matched.”


## Connection To The DataBAse:=

Right Process Is connect To The DataBase Then Listening To The Server
